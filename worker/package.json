{
  "name": "t125-roster",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "node scripts/migrate.mjs --remote",
    "db:init:local": "node scripts/migrate.mjs --local",
    "test": "node --test test/*.test.js",
    "predeploy": "node scripts/preflight.mjs",
    "predb:init": "node scripts/preflight.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
